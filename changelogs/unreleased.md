### ✨ Features
- 

### 🐞 Bug Fixes
- Fix recursion detection scope resolution bug where sequential/nested calls to local helper functions (like `reverse` or `dfs`) were falsely identified as recursive calls to the outer function.
